{
  "name": "@agentscript/agentscript-dialect",
  "version": "2.5.10",
  "description": "AgentScript dialect — schema, lint rules, and dialect config",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "prebuild": "node ../../scripts/sync-pkg-meta.mjs",
    "build": "tsc",
    "dev": "tsc --watch",
    "pretest": "node ../../scripts/sync-pkg-meta.mjs",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage --coverage.reporter=lcov --coverage.reporter=json-summary --coverage.reportsDirectory=coverage",
    "test:watch": "vitest",
    "pretypecheck": "node ../../scripts/sync-pkg-meta.mjs",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentscript/language": "workspace:*",
    "@agentscript/parser": "workspace:*",
    "@agentscript/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.0.0"
  },
  "keywords": [
    "agentscript",
    "dialect",
    "schema",
    "lint"
  ],
  "author": {
    "name": "AgentScript Team",
    "email": "agentscript@salesforce.com"
  },
  "license": "Apache-2.0"
}
