# Config with every field populated
system:
    instructions: "You are a fully configured agent for testing all config fields."
    messages:
        welcome: "Welcome to the fully configured agent."
        error: "We encountered an issue. Please try again later."

config:
    agent_name: "Full_Config_Agent"
    agent_type: "AgentforceServiceAgent"
    developer_name: "Full_Config_Agent_Dev"
    agent_id: "0Xx000000000001"
    default_agent_user: "admin@example.com"
    agent_label: "Full Configuration Agent"
    description: "An agent with every config field populated for testing"

start_agent main:
    description: "Fully configured entry point"
    reasoning:
        instructions: ->
            | Greet the user and help them with their request
