# Empty system messages
system:
    instructions: "You are an agent with empty welcome and error messages."
    messages:
        welcome: ""
        error: ""

config:
    agent_name: "Empty_Messages_Agent"
    agent_type: "AgentforceServiceAgent"
    developer_name: "Empty_Messages_Agent"
    description: "Agent with empty welcome and error messages"

start_agent main:
    description: "Start agent with empty messages"
    reasoning:
        instructions: ->
            | Process the user input
